function downloadGame(gameId) {
    alert('Downloading ' + gameId);
    // Ici, tu pourras ajouter la logique de téléchargement après vérification de l'abonnement
}

function login() {
    // Ici, on ajoutera la logique de connexion avec Firebase
}